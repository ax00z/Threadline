<div class="placeholder">
	<div class="icon">◈</div>
	<p class="title">Network Graph</p>
	<p class="sub">Communication network visualization — Phase 2</p>
</div>

<style>
	.placeholder {
		background: var(--bg-card);
		border-radius: var(--radius);
		padding: 2.5rem 2rem;
		text-align: center;
		border: 1px dashed var(--border);
	}

	.icon {
		font-size: 2rem;
		color: var(--text-muted);
		margin-bottom: 0.5rem;
	}

	.title {
		font-weight: 600;
		color: var(--text-secondary);
		margin-bottom: 0.25rem;
	}

	.sub {
		font-size: 0.8rem;
		color: var(--text-muted);
	}
</style>
